package TestNG.ExplicitWait;

import utils.TestBase;
import org.testng.annotations.Test;

public class ExplicitWait extends TestBase {
    @Test
    public void testExplicitWait() {
//        driver.get("https://the-internet.herokuapp.com/dynamic_controls");
//        driver.findElement(By.xpath("//button[.='Remove']")).click();
//        WebDriverWait wait=new WebDriverWait(driver,10);
//        WebElement message=wait.until(ExpectedConditions.visibilityOfElementLocated(By.id("message")));
//       Assert.assertEquals(message.getText(),"Its gone");
//       WebElement addButton=driver.findElement(By.xpath(("//button[text()='Add']")));
//       addButton.click();
//       wait.until(ExpectedConditions.invisibilityOfElementLocated(By.id("loading")));
//       String actualMessage=driver.findElement(By.id("message")).getText();
//       String expectedMessage="Its back!";
//       Assert.assertEquals(actualMessage,expectedMessage);
//       driver.findElement(By.xpath("//button[.='Remove']")).click();
//
//         }
//
//        }

    }
}